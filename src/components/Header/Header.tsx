import Navigation from './Navigation';

import * as Styled from './Header.styled';

const Header = () => {

  return (
    <Styled.Container>
      <Navigation />
    </Styled.Container>
  );
}

export default Header;